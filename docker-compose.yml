version: "3.9"
services:
  db:
    image: postgres:14.2-alpine3.15
    ports:
      - "5432:5432"
    volumes:
      - "porfolio_data:/var/lib/postgresql/data"
    restart: on-failure
    env_file:
      - '.env.local'
volumes:
  porfolio_data:


